# requirements.txt

# --- Core Flask App ---
Flask
Flask-Cors
flask-caching
Flask-Compress
gunicorn==22.0.0
# uvicorn==0.29.0
# a2wsgi==1.10.10
celery[redis]==5.3.6
redis>=4.5,<6
httpx>=0.23.0
curl_cffi>=0.7.1

# --- Data & Technical Analysis ---
pandas
numpy<2.0
openpyxl
yfinance
TA-Lib
tradingview-datafeed==2.1.1   # installs as tvDatafeed
nsepython==2.97   # NSE/niftyindices direct source for Indian sector indices Yahoo lacks
pandas-market-calendars
tabulate  # For DataFrame.to_markdown() in AI News financial data

# --- Plotting & Visualization ---
matplotlib==3.7.2
plotly
kaleido  # server-side Plotly->PNG export (Technical Agent feeds chart images to GPT-5.4 vision)

# --- AI & API Interaction ---
openai>=1.0.0
google-generativeai>=0.8.6
google-genai
perplexityai
httplib2==0.22.0  # Pinned version with pyparsing 3.x support
pyparsing==3.0.9  # 3.0.x works with both matplotlib<3.1 AND httplib2's DelimitedList

# --- Web Scraping & Document Parsing ---
requests
beautifulsoup4
lxml==5.2.2
html5lib==1.1
pdfplumber
pypdf
python-docx
playwright
playwright-stealth==1.0.6  # <--- USE THIS CORRECT, STABLE VERSION
scrapling[fetchers]
yt-dlp
youtube-transcript-api

# --- Other utilities ---
feedparser
scikit-learn
vaderSentiment==3.3.2
parallel-web

# --- Authentication ---
resend
bcrypt
python-dotenv

# --- Real-time Communication (Budget Live) ---
flask-socketio>=5.3.0
python-socketio>=5.0.0
python-engineio>=4.0.0

# --- Telephony (Live Concall — Exotel: outbound dial, DTMF passcode, recording) ---
# Exotel is called over plain REST via httpx (listed above) — no SDK required.

# --- Live transcription (Tier 2: Exotel Stream applet audio → Deepgram over WS) ---
flask-sock>=0.7.0
deepgram-sdk>=3.0.0
# Only needed by the WS-serving gunicorn worker at deploy time (see plan); installing
# these does not change app behaviour until the worker class is switched.
gevent>=23.0.0
gevent-websocket>=0.10.1

# --- Cosmic Ephemeris Engine ---
pyswisseph
